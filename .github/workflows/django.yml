name: Django Workflow

on:
  workflow_dispatch:

jobs:
  noop:
    name: Manual No-op
    runs-on: ubuntu-latest
    steps:
      - name: No work configured
        run: echo "No automated job configured. Run manually only when needed."
